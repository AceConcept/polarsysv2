# stepscreen

Install from GitHub:

```bash
npm install git+https://github.com/AceConcept/polar-sys.git#main
```

This package exposes the app entry at `stepscreen` and keeps source-path imports available under `stepscreen/src/*`.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`
